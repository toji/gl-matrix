/**
 * Provides optional utilities useful for working with `gl-matrix`.
 *
 * ```js
 * import { toDegree, toRadian } from 'gl-matrix/util';
 *
 * const rad = toRadian(180);
 * ```
 *
 * @packageDocumentation
 */

export * from './angleConversion.js';
