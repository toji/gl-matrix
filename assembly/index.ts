import "assemblyscript/std/portable";
import {
  glMatrix,
  mat2, mat2d, mat3, mat4,
  quat, quat2,
  vec2, vec3, vec4,
} from "./index.as";

export {
  glMatrix,
  mat2, mat2d, mat3, mat4,
  quat, quat2,
  vec2, vec3, vec4,
};
